const router = require('express').Router();
const userRoutes = require('./');
const projectRoutes = require('./');

router.use('/', ____Routes);
router.use('/', _____Routes);

module.exports = router;
